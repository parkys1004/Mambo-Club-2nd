import {StrictMode} from 'react';
import {createRoot} from 'react-dom/client';
import App from './App.tsx';
import './index.css';

// Logic moved to /index.tsx as requested.
// This file is kept to satisfy build/import requirements but does not mount the app.
